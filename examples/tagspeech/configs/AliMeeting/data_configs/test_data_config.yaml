# Relative to examples/tagspeech; use an absolute path if stored elsewhere.
# Audio and feature paths inside the manifest must also exist locally.
- name: alimeeting-test-utterance-group
  manifest: AliMeeting_manifests/alimeeting-far_test_multiASR.jsonl.gz
